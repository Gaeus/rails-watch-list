<h1>Ajouter <%= @movie.title %> à une liste ?</h1>
<h4><%= link_to "Ajouter, modifier ou supprimer une liste", edit_path %></h4>
<div class="lists-container">
  <% @lists.each do |list| %>
    <div class="list-item">
      <p class="list-id"><%= list.id %></p>
      <h2 class="list-name"><%= list.name %></h2>
      <%= form_with url: add_movie_to_list_path, method: :post, local: true do %>
        <%= hidden_field_tag :movie_id, @movie.id %>
        <%= hidden_field_tag :list_id, list.id %>
        <%= submit_tag "Ajouter à cette liste" %>
      <% end %>
    </div>
  <% end %>
</div>
<h5><%= link_to "Retour au menu principal", watchlists_path %></h5>
